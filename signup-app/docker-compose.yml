services:

  backend:
    build: ./backend
    container_name: backend-container
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: frontend-container
    ports:
      - "80:80"
    depends_on:
      - backend